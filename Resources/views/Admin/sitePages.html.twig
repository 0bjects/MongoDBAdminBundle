{% extends 'ObjectsMongoDBAdminBundle:General:base_admin.html.twig' %}

{% block title %}{% trans %}Site Pages{% endtrans %}{% endblock %}

{% block body %}
{% for formError in form.vars.errors %}
    <div class="alert alert-error">
        {{ formError.message }}
    </div>
{% endfor %}
{{ form_start(form) }}
    <legend>About</legend>
    <div class="control-group{% if form.aboutText.vars.errors | length > 0 %} error{% endif %}">
        <div class="controls">
            {{ form_widget(form.aboutText) }}
            <span class="help-inline"></span>
            {% for elementError in form.aboutText.vars.errors %}
            <div class="alert alert-error">
                {{ elementError.message }}<a href="#" class="close" data-dismiss="alert">&times;</a>
            </div>
            {% endfor %}
        </div>
    </div>

    <div class="form-actions">
        <button type="submit" class="btn btn-primary pull-right">{% trans %}Save changes{% endtrans %}</button>
    </div>
{{ form_end(form) }}
{% endblock %}
